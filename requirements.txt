# This file lists the Python packages required to run the examples in this tutorial.
# Install them all at once using pip:
# pip install -r requirements.txt

numpy>=1.21.0
matplotlib>=3.5.0
openpyxl>=3.0.0
